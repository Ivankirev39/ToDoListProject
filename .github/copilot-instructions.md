## Instructions
1. Create a TypeScript function that takes an array of numbers as input.
2. The function should return a new array containing only the even numbers from the input array.
3. Ensure the function is properly typed and includes error handling for non-array inputs.